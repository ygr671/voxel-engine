#pragma once

#include <raylib.h>

/*
class Player
{
private:
  static constexpr Vector3 size_ = (Vector){};
  static constexpr Vector3 collision_box; // maybe delegate this to a dedicated class
  Vector3
}
*/
